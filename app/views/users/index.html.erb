<!--
<h1>Users!!!</h1>
<table class = "table">
  <tr class = "title">
    <td>STT</td>
    <td>User_name</td>
    <td>Role</td>
    <td>Password</td>
    <td>Email</td>
    <td colspan="3"></td>
  </tr>
  <% @users.each do |f| %>
  <tr>
    <td><%= f.id %></td>
    <td><%= f.name_user %></td>
    <td><%= f.role %></td>
    <td><%= f.password %></td>
    <td><%= f.email %></td>
    <td><%= link_to "Show",user_path(f) %></td>
    <td><%= link_to "update",edit_user_path(f) %></td>
    <td><%= link_to "Delete",user_path(f),method: :delete %></td>
  </tr>
  <% end -%>
</table>
<%= link_to "New user",new_user_path%>
-->
<!--
<div class="row">
            <div class="col-md-12 col-sm-12 col-xs-12">
                <div class="x_panel">
                  <div class="x_title">
                    <h2>Table design <small>User Index</small></h2>
                    <ul class="nav navbar-right panel_toolbox">
                      <li><a class="collapse-link"><i class="fa fa-chevron-up"></i></a>
                      </li>
                      <li class="dropdown">
                        <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false"><i class="fa fa-wrench"></i></a>
                        <ul class="dropdown-menu" role="menu">
                          <li><a href="#">Settings 1</a>
                          </li>
                          <li><a href="#">Settings 2</a>
                          </li>
                        </ul>
                      </li>
                      <li><a class="close-link"><i class="fa fa-close"></i></a>
                      </li>
                    </ul>
                    <div class="clearfix"></div>
                  </div>

                  <div class="x_content">
                    <div class="table-responsive">
                      <table class="table table-striped jambo_table bulk_action">
                        <thead>
                          <tr class="headings">
                            <th class="column-title">ID</th>
                            <th class="column-title">User_name</th>
                            <th class="column-title">Role</th>
                            <th class="column-title">Password</th>
                            <th class="column-title">Email</th>
                            <th colspan="3" style="text-align:center;font-size:20px;color:red">Action</th>
                           
                          </tr>
                        </thead>

                        <tbody>
                          <% @users.each do |f| %>
                            <tr>
                              <td><%= f.id %></td>
                              <td><%= f.name_user %></td>
                              <td><%= f.role %></td>
                              <td><%= f.password %></td>
                              <td><%= f.email %></td>
                              <td><%= link_to "Show",user_path(f) %></td>
                              <td><%= link_to "update",edit_user_path(f) %></td>
                              <td><%= link_to "Delete",user_path(f),method: :delete %></td>
                            </tr>
                          <% end -%>
                        </tbody>
                      </table>
                    </div>
                  </div>
                </div>
 </div>    
 <h3><%= link_to "New user",new_user_path , :style => "color:blue" %><h3>   
 -->
<%= datagrid_form_for @grid, :method => :get, :url => users_path %>
<%= will_paginate @grid.assets, renderer: BootstrapPagination::Rails %>
<%= datagrid_table @grid %>
<%= will_paginate @grid.assets, renderer: BootstrapPagination::Rails %>       

