<!--
<h1> Articles </h1>
<table class = "table-backend">
  <tr class = "title">
    <td>STT</td>
    <td>Title</td>
    <td>Details</td>
    <td>Description</td>
    <td>Author</td>
    <td>Comments</td>
    <td>Linkdownload</td>
    <td>Weight</td>
    <td>Image</td>
    <td colspan ="3"></td>
  </tr>
  <% @articles.each do |f| %>
  <tr>
    <td><%= f.id %></td>
    <td><%= f.title %></td>
    <td><%= simple_format(f.details) %></td>
    <td class ="description-article"><%= simple_format(f.description) %></td>
    <td><%= f.user.name_user %></td>
    <td><%= f.comments.count %></td>
    <td><%= f.linkdownload %></td>
    <td><%= f.weight %></td>
    <td><%= image_tag f.avatar.url(:thumb) %></td>
    <td><%= link_to "Show",article_path(f) %></td>
    <td><%= link_to "Update",edit_article_path(f) %></td>
    <td><%= link_to "Delete",article_path(f),method: :delete %></td>
  </tr>
  <% end -%>
</table>
<%= link_to "New Article",new_article_path %>
 -->
 <!--
 <div class="row" >
            <div class="col-md-12 col-sm-12 col-xs-12">
                <div class="x_panel">
                  <div class="x_title">
                    <h2>Table design <small>User Index</small></h2>
                    <ul class="nav navbar-right panel_toolbox">
                      <li><a class="collapse-link"><i class="fa fa-chevron-up"></i></a>
                      </li>
                      <li class="dropdown">
                        <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false"><i class="fa fa-wrench"></i></a>
                        <ul class="dropdown-menu" role="menu">
                          <li><a href="#">Settings 1</a>
                          </li>
                          <li><a href="#">Settings 2</a>
                          </li>
                        </ul>
                      </li>
                      <li><a class="close-link"><i class="fa fa-close"></i></a>
                      </li>
                    </ul>
                    <div class="clearfix"></div>
                  </div>

                  <div class="x_content">
                    <div class="table-responsive">
                      <table class="table table-striped jambo_table bulk_action">
                        <thead>
                          <tr class="headings">
                            <th class="column-title">ID</th>
                            <th class="column-title">Title</th>
                            <th class="column-title">Details</th>
                            <th class="column-title">Description</th>
                            <th class="column-title">Author</th>
                            <th class="column-title">Comments</th>
                            <th class="column-title">Linkdownload</th>
                            <th class="column-title">Weight</th>
                            <th class="column-title">Image</th>
                            <th colspan="3" style="text-align:center;font-size:20px;color:red">Action</th>
                           
                          </tr>
                        </thead>

                        <tbody>
                          <% @articles.each do |f| %>
                            <tr>
                              <td><%= f.id %></td>
                              <td><%= f.title %></td>
                              <td><%= simple_format(f.details.truncate(100)) %></td>
                              <td class ="description-article"><%= simple_format(f.description.truncate(100)) %></td>
                              <td><%= f.user.name_user %></td>
                              <td><%= f.comments.count %></td>
                              <td><%= f.linkdownload.truncate(20) %></td>
                              <td><%= f.weight %></td>
                              <td><%= image_tag f.avatar.url(:thumb) %></td>
                              <td><%= link_to "Show",article_path(f) %></td>
                              <td><%= link_to "Update",edit_article_path(f) %></td>
                              <td><%= link_to "Delete",article_path(f),method: :delete %></td>
                            </tr>
                          <% end -%>
                        </tbody>
                      </table>
                    </div>
                  </div>
                </div>
 </div>    
 <h3><%= link_to "New Article",new_article_path,:style => "color:blue" %></h3>  
 -->
 <%= datagrid_form_for @grid, :method => :get, :url => articles_path %>

<%= paginate(@grid.assets) %>
<%= datagrid_table @grid %>
<%= paginate(@grid.assets) %>
     

